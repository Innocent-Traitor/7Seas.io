/// @description Init Vars

// Do this later
// Essentially just have explosions and fire around <- Do this next
// The ship that died, alongside having the ship
// rotate a random direction, scale down, and slowly
// multiply the blue color so it gets darker bluer
// the longer the animation plays, once it's finished
// stop drawing the sprites and destroy the object

rot = image_angle // Rotation 
dir = irandom(1); // 0 = Counter Clockwise, 1 = Clockwise
spd = random(1); // Speed of animation
xscale = 1;
yscale = 1;
alpha = 1;

alarm[0] = 60;











